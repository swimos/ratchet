jobs:
  - job: autobahnclient
    displayName: Autobahn Client
    pool:
      vmImage: ubuntu-22.04

    steps:
      - script: cargo run --bin client --release
        env:
          RUST_BACKTRACE: 1
        workingDirectory: $(Build.SourcesDirectory)